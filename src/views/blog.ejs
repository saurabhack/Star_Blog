<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <%- include('./partials/tailwind') %>
</head>
<body class="bg-gray-100">
    <%- include('./partials/nav') %>

    <div class="w-full min-h-screen p-6 md:p-10">
        <div class="flex flex-col items-center space-y-6">
            <div class="w-full max-w-4xl">
                <img src="<%= blog.coverImageURL %>" alt="<%= blog.title %>" class="w-full h-auto object-cover rounded-lg shadow-lg">
            </div>

            <div class="w-full max-w-4xl p-4 bg-white rounded-lg shadow-md">
                <h1 class="text-3xl md:text-4xl font-bold text-gray-800 mb-4"><%= blog.title %></h1>
                <p class="text-gray-600 leading-relaxed text-base md:text-lg"><%= blog.body %></p>
            </div>

            <div class="flex items-center space-x-4 mt-8">
                <p class="text-gray-700 font-medium text-lg"> Written By : <%= blog.createdBy.firstName %> <%= blog.createdBy.lastName %></p>
            </div>
        </div>
    </div>
</body>
</html>
